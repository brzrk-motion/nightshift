{
  "name": "Nightshift",
  "install": "pnpm install --frozen-lockfile && pnpm build && { command -v bun >/dev/null 2>&1 || test -x \"$HOME/.bun/bin/bun\" || curl -fsSL https://bun.sh/install | bash; }"
}
