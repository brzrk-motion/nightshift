---
'@nightshift/ui': patch
'@nightshift/dashboard': patch
---

Extend `CommandRegistry.search` with `includeHidden` and `limit` options, and drop the duplicate `filterCommands` helper from the command picker.
