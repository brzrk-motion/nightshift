---
'@nightshift/plugin-shared': patch
'@nightshift/plugin-clock': patch
'@nightshift/plugin-weather': patch
---

Extract shared Open-Meteo geocoding (`geocodeOpenMeteo`, `formatOpenMeteoLocationLabel`) into `@nightshift/plugin-shared` for the clock and weather plugins.
