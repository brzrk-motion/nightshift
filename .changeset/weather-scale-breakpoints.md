---
'@nightshift/plugin-weather': patch
---

Simplify the weather now-widget scale ladder to width/height breakpoints (matching Spotify and ambient-noise). Keep `block` ascii-font off stacked layouts until two 6-row bands fit; keep condition/humidity/wind labels on stacked tiny layouts; keep short compact widgets on plain text without secondary stats or art until they have room.
