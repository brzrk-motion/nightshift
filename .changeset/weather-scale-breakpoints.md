---
'@nightshift/plugin-weather': patch
---

Simplify the weather now-widget scale ladder to width/height breakpoints (matching Spotify and ambient-noise). Keep `block` ascii-font off stacked layouts until the widget is tall enough for two 6-row bands so humidity and wind are not clipped behind `overflow: hidden`.
