---
'@nightshift/ui': patch
---

Export `DASHBOARD_NAME`, `VIBE_NAME`, and `THEME_NAME` from the `@nightshift/ui` package entrypoint so the CLI delete commands can reuse the draft-module name patterns.
