/** @deprecated Use `formContentSize`, `formScale`, and `useFormScale` from `formLayout.js`. */
export {
  formContentSize as vibeEditorContentSize,
  formScale as vibeEditorScale,
  type FormLayout as VibeEditorLayout,
  type FormScale as VibeEditorScale,
} from '../../formLayout.js';
